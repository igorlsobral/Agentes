---
description: Always-on pointers for this context repo. No routing table.
alwaysApply: true
---

# Bootstrap

Read, do not copy:

- `README.md` — standing restrictions
- `agents/process-caller.md` — **only** routing table
- `skills/README.md` — index, then one skill
- `documentation/pendencias.md` — gaps only Igor can fill

Igor is the creator and only approver. Do not invent stubs (`INFORMAÇÃO AUSENTE`). Do not grep outside the allowlist. Do not auto-run `validate-task`. After skill/agent/index changes: `skills/maintain-repo-index.md` + `agents/auditor.md`. After a meaningful change in this repo: commit, then `git push` to `origin` (never force-push, never secrets). After a meaningful change: commit, then push `origin`. Never force-push. Never commit secrets.
