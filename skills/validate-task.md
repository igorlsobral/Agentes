# validate-task

**Manual intake.** Do not auto-run. Tell Igor to invoke this skill when the demand is planned work that is **not** a new video clip.

Footage / “começa um projeto neste bruto” uses [`intake-clip.md`](intake-clip.md) instead.

## When invoked

Create `documentation/.activities/clips/<id>/` only if it is clip-adjacent planned work (scripts, presets, brand). Otherwise propose a new activity kind via [`identify-missing-processes.md`](identify-missing-processes.md) and wait.

Write `overview.md` with:

- Goal
- In-scope / out-of-scope (scope stays closed until Igor reopens it)
- Definition of done
- Links to relevant `documentation/services/` notes
