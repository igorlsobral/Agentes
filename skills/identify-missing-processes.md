# identify-missing-processes

Use after Igor’s actual problem is handled, if:

- the demand fit no skill or process, or
- a workaround repeated, or
- a new domain term appeared.

## Do

1. Propose documenting (skill, agent, process from `_template/`, or a glossary entry).
2. Wait. Igor decides.
3. If approved: write the canonical file, then run [`maintain-repo-index.md`](maintain-repo-index.md) + auditor immediately.
