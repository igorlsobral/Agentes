# export-video

Use when Igor wants the publishable MP4.

## Do

1. Full render is allowed here.
2. Write `projects/<clip>/outputs/` (mp4).
3. Copy the same file to Igor’s Downloads folder (`c:\Users\ig\Downloads\`).
4. **Never delete** `projects/<clip>/` on export.
5. Definition of done: this file came from real footage through the pipeline, not from a unit test.

Show both output paths. Project remains for later second-pass.
