# vsl

Video Sales Letter — **vídeos de vendas**. Canonical term: **VSL** (do not translate).

## Clone the reference (Igor, 2026-08-22)

The long VSL in [`brand/references/`](../brand/references/README.md) is the template. The VSL we build **now** must match it: same beat order, same pacing, same moments where the picture stops for a **frase de impacto**. Future VSLs use that file as the inspiration; this one is a clone.

Before the first graphics pass: **study the reference** (duration, when testimonials end, when each scene beat starts, when each full-screen phrase hits). Write those clocks into the clip overview. Do not invent a faster or slower cut.

### Beat order

1. **Depoimentos** — opening block. Use the files in [`brand/assets/depoimentos/`](../brand/assets/). Do not generate fake testimonials. Play them at the start, in the same count and rhythm as the reference. A 16:9 take inside the 9:16 frame keeps the picture in the middle; top and bottom are a zoomed, blurred copy of that same picture — never white bars (Igor, 2026-08-22).
2. **Body** — the reference uses AI image generation. We do **not**. After testimonials, fill those beats with stretches from [`cenas/`](../cenas/README.md) ([`skills/match-cenas.md`](../skills/match-cenas.md)) that match what is being said. One stretch per spoken idea. Mute library audio; keep our voice (or the reference’s spoken track if this clip’s bruto is that voice).
3. **Frase de impacto** — at the same moments as the reference: the scene **stops**, the frame is **only** the phrase (full screen). Not B-roll with text on top. See [`skills/full-screen-visual.md`](../skills/full-screen-visual.md).

Do not invent price, guarantee, or CTA copy. The reference offer screen (product mockup + green **Order Now**, ~24:00) is structure only. Our price/CTA comes from Igor’s French script in [`transcricao/`](../transcricao/README.md). Until that file is on disk, offer copy stays `INFORMAÇÃO AUSENTE` (P16).

## Talking-head VSL (only if he is on camera)

If the bruto is talking-head and he did **not** ask to clone the reference: cards on the **right**, not over the face; headlines ALL CAPS heavy Coolvetica, supporting text sentence case, each card visually distinct, overlay **60 fps**. On 9:16, keep cards on the right in the lower third. Marked **tela cheia** covers the whole frame until **volta ao normal**.

Captions: `INFORMAÇÃO AUSENTE` (ask if this stage needs them).

Cut feel: clone the reference VSL when that file exists; otherwise `INFORMAÇÃO AUSENTE` (P7).
