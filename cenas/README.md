# cenas

**Role:** Local scene library. Long clips with many shots. The agent matches a spoken beat to a stretch and composites it. Not the talking-head bruto (`raw/`).

**Parent:** [README.md](../README.md)

Drop `.mp4`, `.mov`, or `.mkv` here. Binaries are gitignored; this folder is the library on disk.

Available on **every** format after the rough cut is locked (`long-form`, `short-form-explainer`, `tiktok-raw`, `vsl`) — not only **tela cheia**. Placement follows [`skills/match-cenas.md`](../skills/match-cenas.md). Do not invent a match. Keep the assembly voice; discard library audio unless Igor asks otherwise.

## Inventory

| Entry | When to open | Path |
|---|---|---|
| Library (gitignored) | Match a spoken idea to a visual stretch | this folder |

## Gaps

No files yet until Igor drops them. Naming that hints at the content (place, person, action) helps the match.

## Children

None.
